spring.application.name=fitness-monolith
#server.port=${PORT:8080}
#
#
##Database Configuration
#spring.datasource.url   = ${DB_URL}
#spring.datasource.username = ${DB_USER}
#spring.datasource.password = ${DB_PASSWORD}
#
#
#
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLDialect
#
#
##spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.show-sql= true
#spring.jpa.properties.hibernate.formate_sql = true
#logging.level.org.hibernate.sql=DEBUG
# Database Configuration - uses environment variables
# Server Configuration
# Database Configuration
# Database Configuration
server.port=${PORT:8080}
server.address=0.0.0.0
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# CORS
# application.properties
# Local development + Production dono support
frontend.allowed-origins=http://localhost:5173,https://fintness-monolith.netlify.app

# Agar production mein alag profile use kar rahe ho toh application-prod.properties mein sirf Netlify wala rakh sakte ho

